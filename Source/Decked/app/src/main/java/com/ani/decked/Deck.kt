package com.ani.decked

import java.util.*

class Deck(numOfDecks : Int) {
    val numberOfDecks = numOfDecks
    var deckOfCards : Stack<Card> = getDeck()

    fun getDeck() : Stack<Card> {
        for (i in 1 until numberOfDecks) {
            
        }
    }
}